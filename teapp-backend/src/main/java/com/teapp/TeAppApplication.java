package com.teapp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Clase principal de la aplicación TEApp.
 * Agenda semanal para niños con autismo con administración por parte de los padres.
 */
@SpringBootApplication
public class TeAppApplication {

    public static void main(String[] args) {
        SpringApplication.run(TeAppApplication.class, args);
    }
}
